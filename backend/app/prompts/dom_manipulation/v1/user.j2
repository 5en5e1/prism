## User Request

{{ user_prompt }}
{% if selected_elements_context %}
## Selected Element References

The user selected these live page elements before submitting the prompt:

{{ selected_elements_context }}

These references are precise edit targets. For prompts like "turn this red",
"move this left", "make this thicker", "hide this section", or "change this
button style", apply the change to the selected target described above even
when the prompt does not repeat the quoted reference. Do not guess another
element unless the selected target could not be resolved.
{% endif %}
{% if css_context %}
## Styles (existing design system — keep edits consistent with this)

```css
{{ css_context }}
```
{% endif %}
## Page Skeleton

Each line is one element: `@<anchor id> <tag>.<class>#<domid> [style=… attrs] "text"`.
Target elements by their `@id`. Unpatched elements (and their hidden
descendants/styles/scripts) are preserved exactly.

```
{{ skeleton }}
```

## Task

Produce the minimal set of patch operations that fulfills the request:
"{{ user_prompt }}"

- Prefer `set_text`/`set_attr`/`set_style`/`add_class` over `replace_*`.
- `set_style` merges — only specify properties you want to change.
- Reuse existing classes / CSS variables for visual consistency.

Return ONLY the JSON object described in the system prompt
(`patches` + `changes_summary`). Do not return full page HTML.
